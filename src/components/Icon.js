import React, {Component, useState, useEffect} from 'react';
import {useParams, Link} from 'react-router-dom';

function Icon ({character, setCharacter, info, setInfo, input, setInput}) {

    return (
        <div>This is Icon</div>
    )
}

export default Icon;